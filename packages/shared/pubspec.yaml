name: shared
description: "Shared stuff."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  coordinate_systems_data:
    path: ../coordinate_systems_data
  proj4dart: ^2.1.0
  maps_toolkit: ^3.0.0
  logger: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
