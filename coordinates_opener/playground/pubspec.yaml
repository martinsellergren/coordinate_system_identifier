name: coordinates_opener_playground
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  coordinates_opener:
    path: ..
  shared:
    path: ../../packages/shared
  coordinate_systems:
    path: ../../packages/coordinate_systems

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
