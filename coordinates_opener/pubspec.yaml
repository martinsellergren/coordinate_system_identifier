name: coordinates_opener
description: "Open any coordinates in google maps."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  coordinate_systems_data:
    path: ../packages/coordinate_systems_data

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
